[package]
name = "sudo_game"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.13"
bevy_egui = "0.27"
bevy_renet = "0.0.11"
bevy-steamworks = "0.13"
serde = { version = "1.0", features = ["derive"] }

[features]
default = ["bevy/dynamic_linking"]

[profile.dev.package."*"]
opt-level = 3